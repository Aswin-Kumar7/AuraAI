import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: [
    "physicians-london-realistic-theology.trycloudflare.com ",
    "during-effectiveness-mutual-tours.trycloudflare.com"
  ]
};

export default nextConfig;
