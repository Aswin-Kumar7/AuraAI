import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: [
    "outsourcing-assessing-comparing-baker.trycloudflare.com",
    "activation-dealtime-assumes-bizrate.trycloudflare.com"
  ]
};

export default nextConfig;
