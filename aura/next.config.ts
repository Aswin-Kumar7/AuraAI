import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: [
    "employed-options-routines-avon.trycloudflare.com",
    "himself-prior-allowing-interests.trycloudflare.com"
  ]
};

export default nextConfig;
